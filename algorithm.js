function solve() {
	// Techniques to implement:
	//  Single-celled blocks
	//  Cell that can only have one digit
	//  Only cell in block that can hold a digit
	//  "Cell A can't have digit N because then block B could not have digit N"
	// Recommend having the algorithm track which digit can go into every cell to make this easier.
}

function analyze() {
	// Go through cells and determine which cells cannot have a certain digit
}

function excludeSingles() {
	// Go through cells and find cells that have been determined to only be able to hold a single digit, and fill in those cells
}
